{
  "name": "omni-cms",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web run build:cf",
    "build:cf": "TURBOPACK=0 npx @cloudflare/next-on-pages@1",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint"
  }
}
