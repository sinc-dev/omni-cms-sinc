# ============================================
# ENVIRONMENT CONFIGURATION TEMPLATE
# ============================================
# 
# Next.js automatically loads environment files based on NODE_ENV:
# - .env.development (loaded when NODE_ENV=development)
# - .env.production (loaded when NODE_ENV=production)
# - .env.local (always loaded, gitignored, overrides others)
# 
# For local development:
#   1. Use .env.development (already configured for local dev)
#   2. Create .env.local for secrets (gitignored)
# 
# For production:
#   1. Use .env.production (already configured)
#   2. Set secrets via Cloudflare Pages environment variables
# 
# Alternative: Use .env.dev and .env.prod files (copy to .env.local as needed)

# ============================================
# REQUIRED FOR LOCAL DEV
# ============================================

# Hono API Backend URL
# For local development, the API runs on http://localhost:8787
# This MUST be set to connect the frontend to the local backend
NEXT_PUBLIC_API_URL=http://localhost:8787

# Next.js Frontend URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# CLOUDFLARE CONFIGURATION
# ============================================

# Cloudflare Account
CLOUDFLARE_ACCOUNT_ID=your-account-id
CLOUDFLARE_API_TOKEN=your-api-token

# Cloudflare D1 Database
# Database ID is configured in wrangler.toml
# No separate environment variable needed
# For local dev, wrangler dev uses a local SQLite database automatically

# Cloudflare R2 Storage
# For local dev, you can use .dev.vars in apps/api/ or set these here
R2_ACCOUNT_ID=your-r2-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=omni-cms-media
R2_PUBLIC_URL=https://media.example.com  # Optional: for custom domain

# Cloudflare Access (for authentication)
CF_ACCESS_TEAM_DOMAIN=your-team.cloudflareaccess.com
CF_ACCESS_AUD=your-access-aud
# Public version for client-side (used in sign-in/sign-up pages)
NEXT_PUBLIC_CF_ACCESS_TEAM_DOMAIN=your-team.cloudflareaccess.com

# ============================================
# PRODUCTION OVERRIDES
# ============================================
# Uncomment and update these for production builds:
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com
# NEXT_PUBLIC_APP_URL=https://yourdomain.com
