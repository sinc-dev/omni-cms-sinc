{
  "name": "omni-cms",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "cd web && TURBOPACK=0 next build && npx @cloudflare/next-on-pages --skip-build",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint"
  }
}
