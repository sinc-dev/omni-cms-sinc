# Optimize pnpm installation for Cloudflare Pages builds
prefer-offline=true
side-effects-cache=true
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*

# Additional optimizations for faster builds
dedupe-injected-deps=true

